"use strict"

const user = {
  name: "John"
};

// это будет работать?
user.name = "Pete";

/*
Пример работать будет, так как это не измнение имени объекта - user
*/

name = admin; // ошибка Uncaught ReferenceError